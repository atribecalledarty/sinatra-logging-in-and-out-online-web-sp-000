<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if is_logged_in?(session) %>
      <h1>Username: <%= current_user(session).username %></h1>
      <h2>Balance: <%= current_user(session).balance %></h2>
      <a href="/logout">Log Out</a>
    <% end %>
    <a href=""

  </body>
</html>